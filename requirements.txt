pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
statsmodels>=0.14
pmdarima>=2.0
pyomo>=6.7

# El solver ipopt no se instala vía pip: hace falta el binario en el PATH.
# Se puede instalar con conda (`conda install -c conda-forge ipopt`) o
# descargando el binario desde https://github.com/coin-or/Ipopt/releases
